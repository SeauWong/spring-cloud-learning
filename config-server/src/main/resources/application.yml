spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SeauWong/spring-cloud-learning/
          search-paths: repo
          default-label: master

server:
  port: 8888